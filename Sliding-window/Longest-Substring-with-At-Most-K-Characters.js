/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number}
 */
function longestSubstringWithAtMostKNum(nums, k) {
    const charMap = new Map();
    let l = 0;

    for (let r = 0; r < nums.le)
}